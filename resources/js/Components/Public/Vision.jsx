// Vision component content moved into AboutUs.jsx.
// Keep this file as a lightweight compatibility re-export to avoid breakage
// in case other code still imports `Vision`.
import AboutUs from './AboutUs';

export default AboutUs;
